export enum GameType {
    TETRIS = 'tetris',
}

export enum ShapeType {
    PICACHU = 1,
    FUSHIGI = 2,
    PURIN = 3,
    PIPI = 4,
}

export enum Direction {
    LEFT = 'left',
    RIGHT = 'right',
    DOWN = 'down',
    UP = 'up'
}